# No secrets or environment variables are required to deploy on Vercel.
# Vercel's production domain is used automatically for canonical URLs and sitemap.
# Set this optional value when attaching a verified custom domain (then redeploy):
# NEXT_PUBLIC_SITE_URL=https://www.your-domain.com
